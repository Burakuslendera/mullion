# Verification records — 2026-09

**Status:** active

## Contents

- [2026-09 records](#2026-09-records)
- [Boundary](#boundary)

Earlier dated records live in
[`2026-08.md`](./2026-08.md), routed by the [verification records
index](../records.md). Keep new command results and live observations in this
current-month file; keep acceptance rules and checklist in
[`acceptance-checklist.md`](../acceptance-checklist.md#manual-acceptance-checklist).

## 2026-09 records

### 2026-09-02 — [Issue #140](https://github.com/Burakuslendera/mullion/issues/140) startup-timing Logger re-entry deadlock

- **Automatic, headless Windows evidence / fail-first:** on the unfixed tree `TestStartupTimingSummarySurvivesLoggerReentrantShow` (host/startup_timing_reentry_windows_test.go, no window created) failed in 2.01 s with `recordStartupFrontendReady deadlocked behind a Logger re-entry while startupMu was held`; on the fixed tree it passes in 0.01 s, and control `TestStartupTimingSummaryEmitsOnceForPlainLogger` asserts the summary is still emitted exactly once with a repeated readiness record staying silent. `go build ./...` and `go vet ./...` passed; `go test ./host -run 'TimingSummary|StartupTiming|FrontendReady' -v -count=1` passed 9/9; `go test ./host -count=1` passed (ok 143.9 s); `go test ./... -count=1` passed all 7 packages. The fix splits `logStartupTimingSummary` into `snapshotStartupTimingSummaryLocked` (latching the once-flag under `startupMu`) and `emitStartupTimingSummary` (byte-identical message, after unlock); a full `host.log.*`/host-lock audit found no other Logger-under-lock site. [Decision 0046](../../decisions/0046-logger-never-runs-under-a-host-mutex.md) owns the invariant.
- **Not covered / `unverified`:** no local `-race` run — the CGo-free toolchain has no `gcc` (pre-existing repo-wide gap), so the race lane is not exercised here. [Issue #159](https://github.com/Burakuslendera/mullion/issues/159)'s generation-zero run-call admission re-entry via the `beginRun` drain is a different mechanism, remains open, and is not addressed by this change.
- **Independent review:** a read-only adversarial reviewer re-audited all 188 `host.log` sites against every host-owned mutex (no remaining Logger-under-lock site; `beginRun`'s `WarnCount`/`ErrorCount` reads are atomic counter loads on the host's own sink, not embedder Logger calls), re-verified once-semantics, test soundness, scope (`git diff --stat` shows only the listed files) and docs footers, and re-ran `go build ./...`, `go vet ./...`, the targeted 9/9 run and a full `go test ./host -count=1` (ok, 219.9 s) — all checks passed.
- **Live, real-window Windows evidence (2026-09-02, primary monitor, dpi=96):** a repository-external driver (kept out of the tree on purpose) ran the issue's exact shape live: `New` + `Run` with a `Logger` whose `Info` calls `Show()` when the startup-timing-summary line is emitted. The console shows the summary emit, the re-entrant `Show` entered from inside that same `Info` call, `window visible` 95 ms later, and `Show returned without error — no deadlock`; `frontend ready` followed and the render watchdog did not fire. On the unfixed tree this shape is the deterministic deadlock of the fail-first headless run above. The operator exercised the UI checklist on the same run (content render, drag from an `app-region: drag` header, edge resize, minimise/maximise, snap, page focus) and reported pass; the sole observation was that the page drew no close button, which is the documented design — the frontend draws its own caption buttons and drives them over the bridge — not a defect. The driver's process was ended by the orchestrator rather than closed from the page, so the graceful close path was not exercised live.

### 2026-09-04 — Correction: Issue #140 startup-navigation timing scope

- **Source identity:** this exact file, `docs/verification/records/2026-09.md`,
  and the 2026-09-02 [Issue #140 entry](#2026-09-02-issue-140-startup-timing-logger-re-entry-deadlock)
  above; no new run or artifact is asserted by this correction.
- **Corrected claim:** the Issue #140 re-entry record does not include the
  separate startup-navigation T0/T2 measurement. T0, T2, `T2−T0`, Runtime,
  cold/warm state, run order, raw timing evidence, an approved baseline, and a
  numeric threshold are not recorded; the durations above are not substitutes.
- **Not covered / `unverified`:** startup-navigation timing acceptability
  remains `unverified` because that measurement and approved numeric threshold
  are absent.

### 2026-09-04 — P3 rule-system audit/update

- **Authority and scope:** P3 work was explicitly authorized despite open P0s. This entry covers the current 12 documentation/workflow/template files, with no production lines; protected Tier 3 candidates remain deferred.
- **Final headless suite:** `go test -count=1 ./...` passed for `cmd/mullion`, `host`, `internal/backdrop`, `internal/doctor`, `internal/logsafe`, and `internal/webview2`; `examples/basic` had no tests.
- **Focused checks:** diff integrity, YAML duplicate-key parsing and CI structure, 10 Markdown footer/cap checks, 165 local-link/anchor checks, and an isolated PowerShell helper plus GOOS/GOARCH `finally` cleanup all passed.
- **Independent review:** RulePatchReviewer first found cleanup/source-marker defects; editors fixed them, and the follow-up returned no findings at 0.99 confidence.
- **Not covered / `unverified`:** no GUI, live WebView2, GitHub Actions, or external-link reachability evidence was covered. Startup timing remains `unverified` without an approved numeric threshold.


### 2026-09-04 — verification evidence/headless boundary and documentation cutover

- **Source and authority:** This work used the current dirty working tree at
  HEAD `3a281ab20f660f401e2cca437b67e6cc5e613e48`, under the explicit P2/P3
  override despite 10 open P0s. During the work, an in-scope P0 silent-failure
  in leak-scanner index/worktree selection was reproduced and fixed.
- **Scope:** The change establishes the `docs/verification` router,
  `automated-gates`, `evidence`, `acceptance-checklist`, `diagnostics`,
  `gui-traps`, and `records` tree; moves historical records and exact scanner
  paths; adds decision 0047; updates AGENTS, contributor, policy, issues,
  window, and PR links; preserves behavior while adding host DPI/WM_QUIT and
  WebView2 waiter/loader/machine-test seams; and binds the leak scanner to its
  selected source. Caption/frame production behavior and decision 0003 were
  untouched. Caption-style lifetime remains explanation-only, and
  `.github/ISSUE_TEMPLATE/` remains uppercase and unmoved.

#### `automated_headless` contract line items

- **`DOC-VERIFICATION-ROUTER`**
  - **Production path/symbol:** `docs/verification.md` sole router and
    `docs/verification/` owners.
  - **Exact focused command:** none — this is a documentation responsibility
    move, not an executable path.
  - **Fail-before:** not applicable — behavior was not changed.
  - **Pass-after:** canonical router/owners and moved historical paths were
    recorded; links and one-footer topology were reviewed.
- **`HOST-WM-QUIT` / `LIFECYCLE-TEARDOWN-ORDER` (`GATE-TEST`)**
  - **Production path/symbol:** `host/window_windows.go`:
    `windowExitCleanupDecision`, `destroyWindowOutsideLoop`,
    `drainThreadQuitMessage`.
  - **Exact focused command:** `go test -count=1 ./host -run '^(TestDrainThreadQuitMessageRemovesAPendingQuit|TestWindowExitCleanupDecisionOwnsLiveWindowAndPendingQuit|TestPerMonitorV2DPIAwarenessContext|TestClassifyDPIAwarenessResultPreservesNativeErrorIdentity|TestLeakScanAllowancesBindExactComponents)$'`.
  - **Fail-before:** `unverified` — no pre-fix executable was run and retained;
    source/reviewer evidence only.
  - **Pass-after:** focused host command passed in 72.256 s.
- **`HOST-DPI` / `DPI-MIXED-MONITOR` (`GATE-TEST`)**
  - **Production path/symbol:** `host/dpi_windows.go`:
    `enablePerMonitorV2DPIAwareness`, `classifyDPIAwarenessResult`.
  - **Exact focused command:** `go test -count=1 ./host -run '^(TestDrainThreadQuitMessageRemovesAPendingQuit|TestWindowExitCleanupDecisionOwnsLiveWindowAndPendingQuit|TestPerMonitorV2DPIAwarenessContext|TestClassifyDPIAwarenessResultPreservesNativeErrorIdentity|TestLeakScanAllowancesBindExactComponents)$'`.
  - **Fail-before:** `unverified` — no pre-fix executable was run and retained;
    source/reviewer evidence only.
  - **Pass-after:** focused host command passed in 72.256 s.
- **`WEBVIEW2-LOADER-WAITER` / `LIFECYCLE-REGISTRATION-135` (`GATE-TEST`)**
  - **Production path/symbol:** `internal/webview2/loader_windows.go`:
    `createEnvironmentWithProc`, `Environment.createControllerWithTimeout`;
    `script_completion_windows.go`: `waitForRequiredScriptCompletion`.
  - **Exact focused command:** `go test -count=1 ./internal/webview2 -run '^(TestCreateEnvironmentWithOptionsNativeCallBoundary|TestEnvironmentCreateControllerNativeCallBoundary|TestCompletionDeliveredToTheWaiterKeepsTheReference|TestLateCompletionAfterSuccessfulWaitReleasesTheResult)$'`.
  - **Fail-before:** `unverified` — no pre-fix executable was run and retained;
    source/reviewer evidence only.
  - **Pass-after:** focused loader/completion boundary tests passed; the default
    WebView lane passed in 1.166 s.
- **`WEBVIEW2-DEFAULT-GATE` (`GATE-TEST`)**
  - **Production path/symbol:** `internal/webview2/loader_machine_windows_test.go`:
    `requireWebView2Machine` precedes `FindRuntime`, `RuntimeClientPath`, and
    `DescribeRuntime`.
  - **Exact focused command:** `go test -count=1 ./internal/webview2`.
  - **Fail-before:** `unverified` — no pre-fix executable was run and retained;
    source/reviewer evidence only.
  - **Pass-after:** passed in 1.166 s; all three machine tests skipped before
    access.
- **`LEAK-SCAN-SOURCE-BINDING` (`GATE-LEAK-SCAN`)**
  - **Production path/symbol:** `scripts/leak-scan.ps1`:
    `Get-GitPathList`, `Get-GitManifest`, `Get-SafeWorktreeBytes`; source-selection
    fixtures in `host/leak_scan_test.go`.
  - **Exact focused command:** `go test -count=1 ./host -run '^TestLeakScan'`.
  - **Fail-before:** `observed` — the in-scope P0 silent false-clean from wrong
    index/worktree source selection was reproduced before the fix; no separate
    pre-fix artifact was retained.
  - **Pass-after:** focused `TestLeakScan` suite passed in 217.035 s; final
    canonical scanner also passed.
- **`LEAK-SCAN-BOM-PATH` (`GATE-LEAK-SCAN`)**
  - **Production path/symbol:** `scripts/leak-scan.ps1`:
    `Read-StrictText`, `Read-StrictGitPath`; BOM/path tests in
    `host/leak_scan_test.go`.
  - **Exact focused command:** `go test -count=1 ./host -run '^(TestLeakScanPreservesLeadingBOMGitPathIdentity|TestLeakScanDoesNotLetBOMPathSpoofUseAllowance)$'`.
  - **Fail-before:** `unverified` — no pre-fix executable was run and retained;
    source/reviewer evidence only.
  - **Pass-after:** the two BOM tests passed in 10.985 s; UTF-32 BOM rejection
    remains fail-closed in the focused scanner suite.

#### `runtime_live` / machine evidence

- **`WEBVIEW2-MACHINE` / `GATE-WEBVIEW2-MACHINE`:** Production machine
  discovery/report paths are `FindRuntime`, `RuntimeClientPath`, and
  `DescribeRuntime`; the exact command was
  `MULLION_REQUIRE_WEBVIEW2=1 go test -count=1 ./internal/webview2 -run
  '^(TestFindRuntimeOnThisMachine|TestRuntimeExportsTheEntryPointWeCallDirectly|TestDescribeRuntimeCannotBeSilentAboutTheExport)$'`.
  Fail-before is `unverified` (no pre-fix executable was run and retained;
  source/reviewer evidence only). After the fix, all three machine tests
  passed in 1.001 s. Windows 11/amd64 reported WebView2
  `152.0.4191.62` and the required export.
- **`GATE-DOCTOR`:** `cmd/mullion` production path `doctor`; exact command
  `go run ./cmd/mullion doctor`. Fail-before is `unverified` (no pre-fix
  executable was run and retained; source/reviewer evidence only). After the
  fix, doctor succeeded on Windows 11/amd64 with WebView2
  `152.0.4191.62`, export present, and two monitors at 125% and 100%. This
  proves only direct runtime/export machine discovery, not hosting, rendering,
  or a visible window.

#### `manual_human`, `scripted_gui`, and residual branches

- **`manual_human`:** none — no visual/window-manager/shell/compositor behavior
  changed, so no human GUI checklist action was applicable or run.
- **`scripted_gui`:** none — no GUI harness was run; the closed native-headless
  boundary forbids treating a scripted native-frame action as visual or browser
  evidence.
- **Approved live residual:** none — no residual was proposed; all changed
  contracts were documentation, deterministic seams, machine discovery, or
  source-bound scanner behavior.

#### Supplemental automatic evidence

- `GATE-FORMAT`: `gofmt -l .` produced no output. `GATE-BUILD`:
  `go build ./...` passed. `GATE-VET`: `go vet ./...` passed.
  `GATE-TEST`: `go test -count=1 ./...` passed for `cmd/mullion`, `host`,
  `internal/backdrop`, `internal/doctor`, `internal/logsafe`, and
  `internal/webview2` (`host` 272.632 s); `examples/basic` has no tests.
  `GATE-BRIDGE`: `node scripts/test-bridge.mjs` printed `ok`.
- `GATE-DIAG-DWM-BUILD` / `GATE-DIAG-DWM-TEST` passed with
  `go build -tags mullion_dwm_caption_diag ./...` and
  `go test -count=1 -tags mullion_dwm_caption_diag ./...` (host 232.411 s).
  `GATE-DIAG-CAPTION-PASSTHROUGH-BUILD` /
  `GATE-DIAG-CAPTION-PASSTHROUGH-TEST` passed with the corresponding
  `mullion_caption_passthrough_diag` build/test pair (host 229.859 s).
  `GATE-DIAG-SCRIPT-COMPLETION-BUILD` /
  `GATE-DIAG-SCRIPT-COMPLETION-TEST` passed with the corresponding
  `mullion_script_completion_delay_diag` build/test pair (host 234.675 s).
- `GATE-PORTABLE-LINUX-AMD64` passed
  `GOOS=linux GOARCH=amd64 go build ./...`;
  `GATE-SUPPORTED-WINDOWS-AMD64-BUILD` passed
  `GOOS=windows GOARCH=amd64 go build ./...`;
  `GATE-PORTABLE-WINDOWS-ARM64-BUILD` passed
  `GOOS=windows GOARCH=arm64 go build ./...`; and
  `GATE-UNSUPPORTED-WINDOWS-386-TEST` passed the Windows/386
  unsupported-architecture tests.
- `GATE-LEAK-SCAN`: the final canonical
  `pwsh -NoProfile -File scripts/leak-scan.ps1` passed in 29.42 s with 317
  logical/text files, 365 revisions, 323 HEAD metadata entries, 323 index
  blob entries, 318 worktree files, 5 ordinary-deletion skips, 264 commits,
  1 binary exclusion, 94 explicit synthetic allowances consumed, and 0
  inspection errors. `git diff --check` passed.

#### Coverage limits and review

- **Not covered:** staged-deleted HEAD-only file blobs, untracked files, binary
  contents, unreachable/foreign history, encoded/obfuscated/unknown secret
  classes, GitHub Actions itself, and external URL reachability. `GATE-LIVE-DEMO`
  was not run and is not applicable: no frame/renderer/window behavior changed
  and no test may create a window.
- **Still uncertain:** `GATE-RACE` was attempted as
  `CGO_ENABLED=1 go test -count=1 -race ./...` and failed before tests because
  `gcc` is absent; race evidence is `unverified`, with no retry. The
  unexercised scanner classes and unrun external systems above remain
  `unverified`, not claims of coverage.
- **Review:** Code, documentation, and security reviewers found and fixed
  compile, waiter, gate, environment, classification, link, schema, scanner
  false-clean, BOM, path, and cap defects; bounded final reviews found no
  residual in-scope blocker.


### 2026-09-05 — caption-style timing contract and main publication

- **Authority and contract:** Explicit human Tier-3/P3 approval authorized this
  work despite 10 open P0s. ADR 0048 was added; ADR 0003 remains historical
  and is limitedly superseded only for the creation-time/lifetime boundary.
  Successful normalization means that the required style/frame/DWM native calls
  succeed and post-apply active-profile readback matches. On that path, hidden
  steady state precedes publication, WebView, and `show`. `CreateWindowEx`
  callbacks are outside the guarantee. `Config.Logger` is a diagnostic
  exception. Existing native-failure/profile-mismatch paths log and continue
  and can still reach publication; they are not accepted supported success.
  No production behavior changed in this contract step.
- **Adversarial review:** Review blockers were recorded; final adversarial
  review was **CLEAN**.

#### `automated_headless`

- **Observed gates:** `GATE-FORMAT` — `gofmt -l .` clean (3.05 s);
  `GATE-BUILD` — `go build ./...` PASS (135.18 s); `GATE-VET` —
  `go vet ./...` PASS (132.85 s); `GATE-TEST` —
  `go test -count=1 ./...` PASS (435.98 s: `cmd/mullion` 7.017 s, `host`
  297.281 s, `internal/backdrop` 2.686 s, `internal/doctor` 2.974 s,
  `internal/logsafe` 14.182 s, `internal/webview2` 4.067 s; `examples/basic`
  has no tests); and `GATE-BRIDGE` — `node scripts/test-bridge.mjs` PASS
  (2.65 s).
- **`GATE-LEAK-SCAN`:** The initial staged scan failed on the deliberate prior
  HEAD hash in the newly staged September record. An exact path/value/count
  allowance was added. The focused `go test -count=1 ./host -run ^TestLeakScan`
  then PASS in 232.157 s. The canonical scan PASS with 328 tracked
  logical/text files, 328 content revisions, 323 HEAD missing-classification
  paths, 329 index blob entries, 329 worktree revisions, 0 ordinary deletion
  skips, 264 commits, 1 binary path/file, 98 explicit synthetic allowances
  consumed, and 0 errors.
- **Documentation mechanical evidence:** observed 477 links, only the
  intentional ADR template placeholder, 45 changed Markdown files with one
  footer each, clean caps/TOCs, ADR 1–48/index coherence, no old paths, clean
  YAML, and uppercase `.github/ISSUE_TEMPLATE`.
- **Contract result:** automatic evidence is `observed` for the commands above;
  this record does not claim that native callbacks outside the stated
  normalization path are deterministic headless behavior.

#### `runtime_live`

- none — this is a docs-only contract record; no Runtime or live-window
  observation was run.

#### `manual_human`

- none — no GUI/window behavior changed, so no human display action was
  applicable or run.

#### `scripted_gui`

- none — no GUI harness was run, and no scripted native-frame result is claimed.

- **Not covered:** the race command was not rerun because `gcc` is known
  missing; no GUI/live window, GitHub Actions, or external-link reachability
  evidence was collected. The current fail-open normalization path remains
  outside supported success and unresolved by this decision.
- **Still uncertain (`unverified`):** race, live/GUI, remote Actions, external
  links, and the fail-open normalization path remain unverified; the successful
  normalization contract above is not extrapolated to those paths.

## Boundary
The evidence boundary is defined in
[the canonical evidence document](../evidence.md); this record carries
observations only and makes no normative rule claim.

> Last updated: 2026-09-05 | Editor: OpenAI (GPT-5.6) | Change: record the caption-style timing contract, ADR 0048 publication evidence, and its unresolved coverage boundaries.
